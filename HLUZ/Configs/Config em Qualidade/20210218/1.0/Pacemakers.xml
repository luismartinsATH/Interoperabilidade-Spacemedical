<?xml version="1.0"?>
<root>
  <!-- 
  only pacemakers with translated text which matches those which Sentinel understands: 
  Unknown, None Fitted, Not known if fitted, AAI, AAT, AOO, Atrial, DAT, DDD, DDDR,
  DDI, DDT, DOO, DVI, Dual, VAT, VDD, VDI, VDT, VOO, VVI, VVT, Ventricular. 
  
  nb. pacemakers are only properly managed for the cultures/translations supported by Sentinel.
  
  Any other pacemakers added to those listed below will be treated as unknown types of
  pacemaker (i.e. Sentinel will not know whether they are Atrial, Dual or Ventricular).
  
  Comment out pacemakers tags to exclude them from the drop down list in edit screens, 
  reorder these pacemaker tags to change the order in which they appear in the drop down lists
  -->
  <pacemakers>
    
    <!-- 
    do not delete any of these three pacemaker options because they provide the minimal options
    only one of these three pacemaker options can be defined as the default pacemaker. The
    default pacemaker is defined in CustomSettings.XML. It is defined there because the default
    pacemaker is needed by non-user interface parts of Sentinel.
    -->
    <pacemaker>Desconhecido</pacemaker>
    <pacemaker>Não inserido</pacemaker>
    <pacemaker>Desconhecimento quanto a marca-passo colocado</pacemaker>
    
    <!-- atrial pacemaker options -->
    <pacemaker>AAI</pacemaker>
    <pacemaker>AAT</pacemaker>
    <pacemaker>AOO</pacemaker>
    <pacemaker>Atrial</pacemaker>
    
    <!-- dual (chamber) pacemaker options -->
    <pacemaker>DAT</pacemaker>
    <pacemaker>DDD</pacemaker>
    <pacemaker>DDDR</pacemaker>
    <pacemaker>DDI</pacemaker>
    <pacemaker>DDT</pacemaker>
    <pacemaker>DOO</pacemaker>
    <pacemaker>DVI</pacemaker>
    <pacemaker>Duplo</pacemaker>
    
    <!-- ventricular pacemaker options -->
    <pacemaker>VAT</pacemaker>
    <pacemaker>VDD</pacemaker>
    <pacemaker>VDI</pacemaker>
    <pacemaker>VDT</pacemaker>
    <pacemaker>VOO</pacemaker>
    <pacemaker>VVI</pacemaker>
    <pacemaker>VVT</pacemaker>
    <pacemaker>Ventricular</pacemaker>
  
  </pacemakers>
</root>